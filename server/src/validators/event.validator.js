import { z } from 'zod';

export const createEventSchema = z.object({
  title: z.string().min(1, 'Title is required').max(200),
  description: z.string().optional(),
  type: z.enum([
    'cultural',
    'sports',
    'academic',
    'annual_day',
    'sports_day',
    'holiday',
    'exam',
    'ptm',
    'workshop',
    'celebration',
    'competition',
    'deadline',
    'event',
    'other',
  ]).default('event'),
  startDate: z.string().min(1, 'Start date is required'),
  endDate: z.string().optional().nullable(),
  isFullDay: z.boolean().optional(),
  startTime: z.string().optional().nullable(),
  endTime: z.string().optional().nullable(),
  location: z.string().optional().nullable(),
  audience: z.enum(['all', 'students', 'teachers', 'parents', 'classes']).optional().default('all'),
  targetClasses: z.array(z.string()).optional(),
  status: z.enum(['upcoming', 'ongoing', 'completed', 'cancelled']).optional().default('upcoming'),
  color: z.string().optional(),
});

export const updateEventSchema = z.object({
  title: z.string().min(1).max(200).optional(),
  description: z.string().optional(),
  type: z.enum([
    'cultural',
    'sports',
    'academic',
    'annual_day',
    'sports_day',
    'holiday',
    'exam',
    'ptm',
    'workshop',
    'celebration',
    'competition',
    'deadline',
    'event',
    'other',
  ]).optional(),
  startDate: z.string().optional(),
  endDate: z.string().optional().nullable(),
  isFullDay: z.boolean().optional(),
  startTime: z.string().optional().nullable(),
  endTime: z.string().optional().nullable(),
  location: z.string().optional().nullable(),
  audience: z.enum(['all', 'students', 'teachers', 'parents', 'classes']).optional(),
  targetClasses: z.array(z.string()).optional(),
  status: z.enum(['upcoming', 'ongoing', 'completed', 'cancelled']).optional(),
  color: z.string().optional(),
});

export const updatePhotoCaptionSchema = z.object({
  caption: z.string().max(300, 'Caption cannot exceed 300 characters').optional().default(''),
});
